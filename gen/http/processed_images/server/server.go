// Code generated by goa v3.3.1, DO NOT EDIT.
//
// Processed images HTTP server
//
// Command:
// $ goa gen image-loader/design

package server

import (
	"context"
	processedimages "image-loader/gen/processed_images"
	"net/http"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// Server lists the Processed images service endpoint HTTP handlers.
type Server struct {
	Mounts                         []*MountPoint
	LoadNewProcessedSatelliteImage http.Handler
	GetProcessedSatelliteImage     http.Handler
}

// ErrorNamer is an interface implemented by generated error structs that
// exposes the name of the error as defined in the design.
type ErrorNamer interface {
	ErrorName() string
}

// MountPoint holds information about the mounted endpoints.
type MountPoint struct {
	// Method is the name of the service method served by the mounted HTTP handler.
	Method string
	// Verb is the HTTP method used to match requests to the mounted handler.
	Verb string
	// Pattern is the HTTP request path pattern used to match requests to the
	// mounted handler.
	Pattern string
}

// New instantiates HTTP handlers for all the Processed images service
// endpoints using the provided encoder and decoder. The handlers are mounted
// on the given mux using the HTTP verb and path defined in the design.
// errhandler is called whenever a response fails to be encoded. formatter is
// used to format errors returned by the service methods prior to encoding.
// Both errhandler and formatter are optional and can be nil.
func New(
	e *processedimages.Endpoints,
	mux goahttp.Muxer,
	decoder func(*http.Request) goahttp.Decoder,
	encoder func(context.Context, http.ResponseWriter) goahttp.Encoder,
	errhandler func(context.Context, http.ResponseWriter, error),
	formatter func(err error) goahttp.Statuser,
) *Server {
	return &Server{
		Mounts: []*MountPoint{
			{"LoadNewProcessedSatelliteImage", "POST", "/image-loader/api/v1/processed-images"},
			{"GetProcessedSatelliteImage", "GET", "/image-loader/api/v1/processed-images"},
		},
		LoadNewProcessedSatelliteImage: NewLoadNewProcessedSatelliteImageHandler(e.LoadNewProcessedSatelliteImage, mux, decoder, encoder, errhandler, formatter),
		GetProcessedSatelliteImage:     NewGetProcessedSatelliteImageHandler(e.GetProcessedSatelliteImage, mux, decoder, encoder, errhandler, formatter),
	}
}

// Service returns the name of the service served.
func (s *Server) Service() string { return "Processed images" }

// Use wraps the server handlers with the given middleware.
func (s *Server) Use(m func(http.Handler) http.Handler) {
	s.LoadNewProcessedSatelliteImage = m(s.LoadNewProcessedSatelliteImage)
	s.GetProcessedSatelliteImage = m(s.GetProcessedSatelliteImage)
}

// Mount configures the mux to serve the Processed images endpoints.
func Mount(mux goahttp.Muxer, h *Server) {
	MountLoadNewProcessedSatelliteImageHandler(mux, h.LoadNewProcessedSatelliteImage)
	MountGetProcessedSatelliteImageHandler(mux, h.GetProcessedSatelliteImage)
}

// MountLoadNewProcessedSatelliteImageHandler configures the mux to serve the
// "Processed images" service "Load new processed satellite image" endpoint.
func MountLoadNewProcessedSatelliteImageHandler(mux goahttp.Muxer, h http.Handler) {
	f, ok := h.(http.HandlerFunc)
	if !ok {
		f = func(w http.ResponseWriter, r *http.Request) {
			h.ServeHTTP(w, r)
		}
	}
	mux.Handle("POST", "/image-loader/api/v1/processed-images", f)
}

// NewLoadNewProcessedSatelliteImageHandler creates a HTTP handler which loads
// the HTTP request and calls the "Processed images" service "Load new
// processed satellite image" endpoint.
func NewLoadNewProcessedSatelliteImageHandler(
	endpoint goa.Endpoint,
	mux goahttp.Muxer,
	decoder func(*http.Request) goahttp.Decoder,
	encoder func(context.Context, http.ResponseWriter) goahttp.Encoder,
	errhandler func(context.Context, http.ResponseWriter, error),
	formatter func(err error) goahttp.Statuser,
) http.Handler {
	var (
		decodeRequest  = DecodeLoadNewProcessedSatelliteImageRequest(mux, decoder)
		encodeResponse = EncodeLoadNewProcessedSatelliteImageResponse(encoder)
		encodeError    = EncodeLoadNewProcessedSatelliteImageError(encoder, formatter)
	)
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := context.WithValue(r.Context(), goahttp.AcceptTypeKey, r.Header.Get("Accept"))
		ctx = context.WithValue(ctx, goa.MethodKey, "Load new processed satellite image")
		ctx = context.WithValue(ctx, goa.ServiceKey, "Processed images")
		payload, err := decodeRequest(r)
		if err != nil {
			if err := encodeError(ctx, w, err); err != nil {
				errhandler(ctx, w, err)
			}
			return
		}
		res, err := endpoint(ctx, payload)
		if err != nil {
			if err := encodeError(ctx, w, err); err != nil {
				errhandler(ctx, w, err)
			}
			return
		}
		if err := encodeResponse(ctx, w, res); err != nil {
			errhandler(ctx, w, err)
		}
	})
}

// MountGetProcessedSatelliteImageHandler configures the mux to serve the
// "Processed images" service "Get processed satellite image" endpoint.
func MountGetProcessedSatelliteImageHandler(mux goahttp.Muxer, h http.Handler) {
	f, ok := h.(http.HandlerFunc)
	if !ok {
		f = func(w http.ResponseWriter, r *http.Request) {
			h.ServeHTTP(w, r)
		}
	}
	mux.Handle("GET", "/image-loader/api/v1/processed-images", f)
}

// NewGetProcessedSatelliteImageHandler creates a HTTP handler which loads the
// HTTP request and calls the "Processed images" service "Get processed
// satellite image" endpoint.
func NewGetProcessedSatelliteImageHandler(
	endpoint goa.Endpoint,
	mux goahttp.Muxer,
	decoder func(*http.Request) goahttp.Decoder,
	encoder func(context.Context, http.ResponseWriter) goahttp.Encoder,
	errhandler func(context.Context, http.ResponseWriter, error),
	formatter func(err error) goahttp.Statuser,
) http.Handler {
	var (
		decodeRequest  = DecodeGetProcessedSatelliteImageRequest(mux, decoder)
		encodeResponse = EncodeGetProcessedSatelliteImageResponse(encoder)
		encodeError    = EncodeGetProcessedSatelliteImageError(encoder, formatter)
	)
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := context.WithValue(r.Context(), goahttp.AcceptTypeKey, r.Header.Get("Accept"))
		ctx = context.WithValue(ctx, goa.MethodKey, "Get processed satellite image")
		ctx = context.WithValue(ctx, goa.ServiceKey, "Processed images")
		payload, err := decodeRequest(r)
		if err != nil {
			if err := encodeError(ctx, w, err); err != nil {
				errhandler(ctx, w, err)
			}
			return
		}
		res, err := endpoint(ctx, payload)
		if err != nil {
			if err := encodeError(ctx, w, err); err != nil {
				errhandler(ctx, w, err)
			}
			return
		}
		if err := encodeResponse(ctx, w, res); err != nil {
			errhandler(ctx, w, err)
		}
	})
}
